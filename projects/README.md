# Project media

Drop video (`.mp4`) and image (`.jpg/.png`) files here. Reference them from
`src/projects-data.js` as `/projects/<filename>` (the `public/` prefix is dropped
by Vite at serve/build time).
